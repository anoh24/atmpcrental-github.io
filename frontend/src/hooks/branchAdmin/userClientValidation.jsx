// import { apiUpdateUserClient } from "../api/updateUserClient";

// export const useUpdateUserClient = () => {
//   const handleValidation = async (customerId, formData) => {
//     try {
//       console.log("Sending data to backend:", formData);
//       const response = await apiUpdateUserClient(customerId, formData);
//       console.log("Response:", response.data);
//     } catch (error) {
//       console.error("Update failed:", error);
//     }
//   };

//   return { handleValidation };
// };
